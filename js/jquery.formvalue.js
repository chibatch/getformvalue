// Generated by CoffeeScript 1.3.1
var $;

$ = jQuery;

$.fn.getFormValue = function() {
  var data, inputs;
  inputs = $(this).find(':input');
  data = {};
  inputs.each(function() {
    var count, name, selector, tag, type, value, values;
    tag = this.tagName.toLowerCase();
    name = this.name;
    if (tag === 'input') {
      type = $(this).attr('type');
      if (type === 'radio') {
        if ($(this).is(':checked')) {
          value = $(this).val();
        }
      } else if (type === 'checkbox') {
        selector = 'input[name="' + name + '"]';
        count = $(selector).size();
        if (count === 1) {
          if ($(this).is(':checked')) {
            value = $(this).val();
          }
        } else if (count > 1) {
          values = [];
          $(selector + ':checked').each(function() {
            return values.push($(this).val());
          });
          if (values.length > 1) {
            value = values;
          }
        }
      } else if (type === 'number') {
        value = Number($(this).val());
      } else {
        value = $(this).val();
      }
    } else {
      value = $(this).val();
    }
    if (value && name) {
      return data[name] = value;
    }
  });
  return data;
};
